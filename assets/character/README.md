# Character assets

Place your provided character images here:

- `explorer-sheet.png` → main full-body character sheet image with white background.

The runtime in `script.js` automatically removes white/light background pixels and uses the character as the interactive stage avatar.

If the image is missing, the experience falls back to a procedural sci-fi avatar.
